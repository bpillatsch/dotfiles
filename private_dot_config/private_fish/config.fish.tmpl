if status is-interactive
  #starship init fish | source
  atuin init fish  --disable-up-arrow | source
end

if status is-login
  if test -e $HOME/bin/wsl2-ssh-agent
    $HOME/bin/wsl2-ssh-agent | source
  end
end
